import React,{useState} from 'react';
import {request} from '../api/client';
export function Auth({onLogin}:{onLogin:()=>void}){
 const[register,setRegister]=useState(location.pathname==='/register'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);
 const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await request(`/api/auth/${register?'register':'login'}`,'POST',{email,password});history.replaceState({},'','/assistant');onLogin();}catch(e){setError((e as Error).message);}finally{setBusy(false);}};
 return <main className="auth-layout"><section className="auth-story"><a className="auth-brand" href="/">halyk<span>●</span><small>AI КӨМЕКШІ · DEMO</small></a><div><span className="gold-label">ПРОСТО. ПОНЯТНО. РЯДОМ.</span><h1>Ваш голос.<br/>Ваш помощник.</h1><p>Спрашивайте на русском или қазақша.<br/>Разберёмся вместе — шаг за шагом.</p></div><div className="auth-features"><span>◉ Живой диалог</span><span>↗ Наглядные подсказки</span><span>▤ История разговоров</span></div><small>Независимый учебный проект. Не официальный сервис Halyk Bank.</small></section><section className="auth-form"><span className="eyebrow">ДОБРО ПОЖАЛОВАТЬ</span><h2>{register?'Создайте аккаунт':'Рады вас видеть'}</h2><p>{register?'Сохраняйте разговоры и продолжайте в удобное время.':'Войдите, чтобы продолжить разговор.'}</p><form onSubmit={submit}><label htmlFor="email">Email / Gmail</label><input id="email" type="email" autoComplete="email" required value={email} maxLength={254} placeholder="you@gmail.com" onChange={e=>setEmail(e.target.value)}/><label htmlFor="password">Пароль</label><input id="password" type="password" autoComplete={register?'new-password':'current-password'} required minLength={10} maxLength={128} placeholder="Не менее 10 символов" value={password} onChange={e=>setPassword(e.target.value)}/>{register&&<p className="auth-note">Придумайте отдельный пароль для этого демосайта. Пароль от Gmail или банка здесь не нужен.</p>}{error&&<div className="error" role="alert">{error}</div>}<button className="primary" disabled={busy}>{busy?'Подождите…':register?'Создать аккаунт →':'Войти →'}</button></form><button className="text-button" onClick={()=>{setRegister(!register);setError('');history.replaceState({},'',register?'/login':'/register');}}>{register?'Уже есть аккаунт? Войти':'Нет аккаунта? Зарегистрироваться'}</button><p className="auth-note">Разговоры сохраняются в вашем аккаунте. Их можно удалить в разделе «История». Голос ответов создан ИИ.</p></section></main>;
}
